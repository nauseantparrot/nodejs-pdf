// Error messages configuration
const pdfErrorMessages = {
    valIsNotType: type => 'The passed value is not of ' + type + ' type',
    valMustStartWith: char => 'The passed value must start with the ' + char + ' character',
}

module.exports = pdfErrorMessages